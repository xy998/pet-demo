import { useEffect, useState } from 'react';
import type { FormEvent, ReactNode } from 'react';
import { api } from './api';
import type { Memory, Pet, User } from './api';
import './App.css';

type Route = { name: string; id?: string; slug?: string };
function getRoute(): Route { const parts = location.pathname.replace(/^\/+|\/+$/g, '').split('/'); if (!parts[0]) return { name: 'home' }; if (['login', 'register'].includes(parts[0])) return { name: parts[0] }; if (parts[0] === 'p') return { name: 'public', slug: parts[1] }; if (parts[0] === 'dashboard') return { name: parts[1] || 'dashboard', id: parts[2] }; return { name: 'home' }; }
function navigate(path: string) { history.pushState({}, '', path); dispatchEvent(new PopStateEvent('popstate')); }
function Image({ src, alt, className = '' }: { src?: string | null; alt: string; className?: string }) { return src ? <img src={src} alt={alt} className={className} /> : <div className={`${className} image-placeholder`} aria-label={alt}>✦</div>; }
function Notice({ text }: { text: string }) { return <div className="notice error">{text}</div>; }
function Loading() { return <div className="loading"><span />正在加载</div>; }
function Empty({ title, text, action, onAction }: { title: string; text: string; action: string; onAction: () => void }) { return <div className="empty"><span>✦</span><h2>{title}</h2><p>{text}</p><button className="button primary" onClick={onAction}>{action} →</button></div>; }
function Layout({ user, children, onLogout }: { user: User | null; children: ReactNode; onLogout: () => void }) { return <div className="site"><header className="nav"><button className="wordmark" onClick={() => navigate('/')}>✦ <b>宠物时光</b></button><nav><button onClick={() => navigate('/dashboard')}>我的宠物</button><button onClick={() => navigate('/')}>关于</button>{user ? <><span className="user-chip">{user.email.slice(0, 1).toUpperCase()}</span><button className="text-button" onClick={onLogout}>退出</button></> : <button className="nav-login" onClick={() => navigate('/login')}>登录</button>}</nav><button className="mobile-menu">☰</button></header>{children}</div>; }
function Home() { return <main><section className="hero"><div className="hero-copy"><span className="eyebrow">A LITTLE LIFE, A LOT OF LOVE</span><h1>每一天的陪伴，<br /><em>都值得好好收藏。</em></h1><p>一张属于 TA 的名片，一本属于你们的回忆录。<br />从第一次见面，到一起慢慢长大。</p><div className="hero-actions"><button className="button primary" onClick={() => navigate('/register')}>创建我们的故事 ↗</button><button className="text-button" onClick={() => navigate('/login')}>登录后开始 →</button></div><small>专属名片 · 图文回忆 · 密码分享</small></div><div className="hero-card"><div className="card-top"><span>✦ PET JOURNAL</span><span>YOUR STORY</span></div><div className="hero-avatar">🐾</div><h2>把陪伴写成一封信</h2><p>头像、性格和每个想珍藏的瞬间，组成一张属于 TA 的成长名片。</p><div className="hero-line"><span>MEMORIES</span><b>一页页，慢慢长大</b></div></div></section><section className="features"><Feature n="01 / PROFILE" title="认识独一无二的 TA" text="头像、性格和小小偏爱，组成专属的成长名片。" icon="◒" /><Feature n="02 / MEMORIES" title="把回忆一页页留下" text="用文字和照片，记录每一个想珍藏的瞬间。" icon="✧" /><Feature n="03 / SHARE" title="分享给在乎的人" text="公开分享或设置访问密码，让亲友一起见证成长。" icon="↗" /></section></main>; }
function Feature({ n, title, text, icon }: { n: string; title: string; text: string; icon: string }) { return <article className="feature"><span className="feature-icon">{icon}</span><span className="eyebrow">{n}</span><h3>{title}</h3><p>{text}</p></article>; }
function Auth({ mode, onAuth }: { mode: 'login' | 'register'; onAuth: (u: User) => void }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [birthday, setBirthday] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const submit = async (e: FormEvent) => { e.preventDefault(); setError(''); if (password.length < 12) return setError('密码至少需要 12 位'); setBusy(true); try { const result = mode === 'login' ? await api.login(email, password) : await api.register(email, password, birthday); onAuth(result.user); navigate('/dashboard'); } catch (err) { setError(err instanceof Error ? err.message : '操作失败'); } finally { setBusy(false); } }; return <main className="auth-page"><div className="auth-card"><span className="brand-star">✦</span><span className="eyebrow">{mode === 'login' ? 'WELCOME BACK' : 'START YOUR STORY'}</span><h1>{mode === 'login' ? '欢迎回来' : '创建你的账号'}</h1><p>{mode === 'login' ? '继续记录你们一起长大的每一天。' : '从一张名片开始，为 TA 留下一个温柔的角落。'}</p><form onSubmit={submit}><label>邮箱<input type="email" value={email} onChange={e => setEmail(e.target.value)} required placeholder="you@example.com" /></label><label>密码<input type="password" value={password} onChange={e => setPassword(e.target.value)} required minLength={12} placeholder="至少 12 位" /></label>{mode === 'register' && <label>生日 <small>选填</small><input type="date" value={birthday} onChange={e => setBirthday(e.target.value)} /></label>}{error && <p className="error">{error}</p>}<button className="button primary full" disabled={busy}>{busy ? '处理中…' : mode === 'login' ? '登录 →' : '创建账号 →'}</button></form><p className="auth-switch">{mode === 'login' ? '还没有账号？' : '已经有账号？'} <button onClick={() => navigate(mode === 'login' ? '/register' : '/login')}>{mode === 'login' ? '创建账号' : '去登录'}</button></p></div></main>; }
function Dashboard({ user }: { user: User }) { const [pets, setPets] = useState<Pet[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { api.pets().then(setPets).catch(e => setError(e.message)).finally(() => setLoading(false)); }, []); return <main className="content"><div className="heading-row"><div><span className="eyebrow">YOUR LITTLE WORLD</span><h1>我的宠物</h1><p>把每一个值得记住的日子，好好放在这里。</p></div><button className="button primary" onClick={() => navigate('/dashboard/new')}>＋ 添加宠物</button></div>{loading && <Loading />}{error && <Notice text={error} />}{!loading && !error && pets.length === 0 && <Empty title="还没有宠物名片" text="创建第一张名片，把 TA 的故事留在这里。" action="创建宠物" onAction={() => navigate('/dashboard/new')} />}{pets.map(pet => <PetCard key={pet.id} pet={pet} />)}<div className="account-caption">当前登录：{user.email}</div></main>; }
function PetCard({ pet }: { pet: Pet }) { return <article className="pet-card"><Image src={pet.avatarUrl} alt={pet.name} className="pet-avatar" /><div className="pet-content"><span className={`pill ${pet.isPublished ? 'green' : 'muted-pill'}`}>{pet.isPublished ? '● 已发布' : '● 草稿'}</span><h2>{pet.name}</h2><p>{pet.breed || '未设置品种'} · {pet.viewCount || 0} 次查看</p><div className="card-links"><button className="button soft" onClick={() => navigate(`/dashboard/edit/${pet.id}`)}>编辑资料</button><button className="text-button" onClick={() => navigate(`/dashboard/memories/${pet.id}`)}>成长回忆 →</button><button className="text-button" onClick={() => navigate(`/dashboard/share/${pet.id}`)}>分享 →</button></div></div></article>; }
function Editor({ id }: { id?: string }) { const isNew = !id; const [form, setForm] = useState<Partial<Pet>>({ name: '', species: 'dog', breed: '', bio: '', birthday: '', avatarUrl: '' }); const [busy, setBusy] = useState(!isNew); const [error, setError] = useState(''); useEffect(() => { if (id) api.pet(id).then(setForm).catch(e => setError(e.message)).finally(() => setBusy(false)); }, [id]); const update = (key: string, value: string) => setForm(prev => ({ ...prev, [key]: value })); const submit = async (e: FormEvent) => { e.preventDefault(); setBusy(true); try { const pet = isNew ? await api.createPet(form) : await api.updatePet(id!, form); navigate(`/dashboard/edit/${pet.id}`); } catch (err) { setError(err instanceof Error ? err.message : '保存失败'); } finally { setBusy(false); } }; if (busy && !isNew) return <main className="content"><Loading /></main>; return <main className="content narrow"><button className="back" onClick={() => navigate('/dashboard')}>← 返回我的宠物</button><div className="form-title"><span className="eyebrow">PROFILE</span><h1>{isNew ? '创建一张名片' : '编辑资料'}</h1><p>让 TA 的故事，从一个名字开始。</p></div><form className="panel form" onSubmit={submit}><label>名字<input value={form.name || ''} onChange={e => update('name', e.target.value)} required placeholder="例如：六一" /></label><div className="two-col"><label>类型<select value={form.species || 'dog'} onChange={e => update('species', e.target.value)}><option value="dog">狗狗</option><option value="cat">猫咪</option><option value="other">其他</option></select></label><label>品种 <small>选填</small><input value={form.breed || ''} onChange={e => update('breed', e.target.value)} placeholder="例如：柴犬" /></label></div><label>关于 TA <small>选填</small><textarea rows={6} value={form.bio || ''} onChange={e => update('bio', e.target.value)} placeholder="写下 TA 的性格、喜欢的事物……" /></label><div className="two-col"><label>生日 <small>选填</small><input type="date" value={form.birthday || ''} onChange={e => update('birthday', e.target.value)} /></label><label>头像地址 <small>选填</small><input type="url" value={form.avatarUrl || ''} onChange={e => update('avatarUrl', e.target.value)} placeholder="https://…" /></label></div>{error && <p className="error">{error}</p>}<div className="form-actions"><button type="button" className="button soft" onClick={() => navigate('/dashboard')}>取消</button><button className="button primary" disabled={busy}>{busy ? '保存中…' : '保存资料 →'}</button></div></form></main>; }
function Memories({ id }: { id: string }) { const [pet, setPet] = useState<Pet | null>(null); const [items, setItems] = useState<Memory[]>([]); const [show, setShow] = useState(false); const [error, setError] = useState(''); useEffect(() => { Promise.all([api.pet(id), api.memories(id)]).then(([p, m]) => { setPet(p); setItems(m); }).catch(e => setError(e.message)); }, [id]); const remove = async (memoryId: string) => { await api.deleteMemory(id, memoryId); setItems(list => list.filter(item => item.id !== memoryId)); }; if (!pet) return <main className="content">{error ? <Notice text={error} /> : <Loading />}</main>; return <main className="content"><button className="back" onClick={() => navigate('/dashboard')}>← 返回我的宠物</button><div className="heading-row"><div><span className="eyebrow">PET JOURNAL</span><h1>{pet.name}的成长回忆</h1><p>每一次第一次，都值得留下。</p></div><button className="button primary" onClick={() => setShow(true)}>＋ 写一篇回忆</button></div>{items.length === 0 ? <Empty title="故事才刚刚开始" text="写下第一个值得收藏的瞬间。" action="写一篇回忆" onAction={() => setShow(true)} /> : <div className="memory-grid">{items.map(memory => <article className="memory-card" key={memory.id}><div className="memory-head"><div><span className="eyebrow">{memory.date || '未设置日期'}</span><h2>{memory.title}</h2></div><button className="delete" onClick={() => remove(memory.id)}>删除</button></div><p>{memory.text}</p><div className="memory-images">{memory.photos.map(photo => <img key={photo} src={photo} alt="" />)}</div></article>)}</div>}{show && <MemoryForm petId={id} onClose={() => setShow(false)} onSaved={memory => { setItems(list => [memory, ...list]); setShow(false); }} />}</main>; }
function MemoryForm({ petId, onClose, onSaved }: { petId: string; onClose: () => void; onSaved: (memory: Memory) => void }) { const [title, setTitle] = useState(''); const [date, setDate] = useState(''); const [text, setText] = useState(''); const [error, setError] = useState(''); const submit = async (e: FormEvent) => { e.preventDefault(); try { onSaved(await api.createMemory(petId, { title, date: date || null, text, photos: [] })); } catch (err) { setError(err instanceof Error ? err.message : '保存失败'); } }; return <div className="modal-layer"><form className="modal panel" onSubmit={submit}><div className="modal-head"><div><span className="eyebrow">NEW MEMORY</span><h2>记录一个瞬间</h2></div><button type="button" className="close" onClick={onClose}>×</button></div><label>标题<input value={title} onChange={e => setTitle(e.target.value)} required placeholder="例如：第一次见面" /></label><label>日期<input type="date" value={date} onChange={e => setDate(e.target.value)} /></label><label>写点什么<textarea value={text} onChange={e => setText(e.target.value)} required rows={5} placeholder="那天发生了什么？" /></label>{error && <p className="error">{error}</p>}<div className="form-actions"><button type="button" className="button soft" onClick={onClose}>取消</button><button className="button primary">保存回忆 →</button></div></form></div>; }
function Share({ id }: { id: string }) { const [pet, setPet] = useState<Pet | null>(null); const [visibility, setVisibility] = useState<Pet['visibility']>('PUBLIC'); const [password, setPassword] = useState(''); const [message, setMessage] = useState(''); useEffect(() => { api.pet(id).then(p => { setPet(p); setVisibility(p.visibility); }).catch(e => setMessage(e.message)); }, [id]); if (!pet) return <main className="content">{message ? <Notice text={message} /> : <Loading />}</main>; const save = async () => { try { await api.sharing(id, { visibility, password: password || undefined }); setMessage('分享设置已保存'); } catch (e) { setMessage(e instanceof Error ? e.message : '保存失败'); } }; const url = `${location.origin}/p/${pet.slug}`; return <main className="content narrow"><button className="back" onClick={() => navigate('/dashboard')}>← 返回我的宠物</button><div className="form-title"><span className="eyebrow">SHARE YOUR STORY</span><h1>分享 {pet.name}</h1><p>让在乎的人，也能看见 TA 的成长。</p></div><div className="panel share-panel"><div className="share-preview"><Image src={pet.avatarUrl} alt={pet.name} className="share-avatar" /><div><b>{pet.name}的成长记录</b><span>/p/{pet.slug}</span></div></div><label>访问方式<select value={visibility} onChange={e => setVisibility(e.target.value as Pet['visibility'])}><option value="PUBLIC">公开访问</option><option value="PASSWORD">密码访问</option></select></label>{visibility === 'PASSWORD' && <label>访问密码<input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="至少 4 位" /></label>}<div className="share-url"><input readOnly value={url} /><button type="button" onClick={() => { navigator.clipboard?.writeText(url); setMessage('链接已复制'); }}>复制</button></div><button className="button primary full" onClick={save}>保存分享设置 →</button>{message && <p className="success">{message}</p>}</div></main>; }
function Public({ slug }: { slug: string }) { const [pet, setPet] = useState<Pet | null>(null); const [password, setPassword] = useState(''); const [locked, setLocked] = useState(false); const [error, setError] = useState(''); useEffect(() => { api.publicPet(slug).then(p => { setPet(p); setLocked(p.visibility === 'PASSWORD'); }).catch(e => setError(e.message)); }, [slug]); const unlock = async () => { try { setPet(await api.unlock(slug, password)); setLocked(false); } catch (e) { setError(e instanceof Error ? e.message : '密码错误'); } }; if (!pet) return <main className="public">{error ? <Notice text={error} /> : <Loading />}</main>; if (locked) return <main className="public"><div className="locked panel"><span className="brand-star">✦</span><h1>这是一张私密名片</h1><p>输入访问密码，看看 {pet.name} 的成长记录。</p><input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="访问密码" /><button className="button primary full" onClick={unlock}>进入名片 →</button>{error && <p className="error">{error}</p>}</div></main>; return <main className="public"><article className="public-sheet"><button className="wordmark" onClick={() => navigate('/')}>✦ <b>宠物时光</b></button><div className="public-intro"><Image src={pet.avatarUrl} alt={pet.name} className="public-avatar" /><h1>{pet.name}</h1><p>{pet.bio || '每一个日常，都值得被珍藏。'}</p></div><div className="public-stats"><div><b>{pet.breed || '—'}</b><span>品种</span></div><div><b>{pet.memories?.length || 0}</b><span>篇回忆</span></div><div><b>{pet.viewCount || 0}</b><span>次查看</span></div></div><section><div className="section-title"><h2>关于{pet.name}</h2><span>ABOUT</span></div><p className="public-bio">{pet.bio || '故事正在慢慢发生。'}</p></section><section><div className="section-title"><h2>我们的回忆</h2><span>MEMORIES</span></div>{pet.memories?.map(memory => <article className="public-memory" key={memory.id}><span className="eyebrow">{memory.date || '未设置日期'}</span><h3>{memory.title}</h3><p>{memory.text}</p><div>{memory.photos.map(photo => <img key={photo} src={photo} alt="" />)}</div></article>)}</section><blockquote>“有你的每一天，都是值得收藏的回忆。”<cite>— 爱你的家人</cite></blockquote></article></main>; }

export default function App() { const [current, setCurrent] = useState(getRoute()); const [user, setUser] = useState<User | null>(null); const [ready, setReady] = useState(false); useEffect(() => { const listener = () => setCurrent(getRoute()); addEventListener('popstate', listener); api.me().then(result => setUser(result.user)).catch(() => setUser(null)).finally(() => setReady(true)); return () => removeEventListener('popstate', listener); }, []); const logout = async () => { await api.logout().catch(() => undefined); setUser(null); navigate('/'); }; const protectedRoute = ['dashboard', 'new', 'edit', 'memories', 'share'].includes(current.name); if (!ready && protectedRoute) return <div className="site"><Loading /></div>; if (protectedRoute && !user) { navigate('/login'); return null; } let view: ReactNode; if (current.name === 'home') view = <Home />; else if (current.name === 'login' || current.name === 'register') view = <Auth mode={current.name} onAuth={setUser} />; else if (current.name === 'dashboard') view = <Dashboard user={user!} />; else if (current.name === 'new' || current.name === 'edit') view = <Editor id={current.id} />; else if (current.name === 'memories') view = <Memories id={current.id!} />; else if (current.name === 'share') view = <Share id={current.id!} />; else view = <Public slug={current.slug!} />; return ['public', 'login', 'register'].includes(current.name) ? <div className="site">{view}</div> : <Layout user={user} onLogout={logout}>{view}</Layout>; }
